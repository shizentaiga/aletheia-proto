# ALETHEIA（アレテイア）

> **「つながりは、偶然から。個人の記録を起点とした地点検索プラットフォーム」**

ALETHEIAは、効率性や画一的な検索アルゴリズムに依存しない、直感的な地点発見体験を提供します。
Google Maps APIを「初期の発見ツール」として活用し、ユーザーの熱量（記録・編集）を通じてデータを「自社資産」へと昇華させる、自律型データベース・プラットフォームです。

---

## 0. ドキュメント体系
プロジェクトの詳細な仕様は以下の各ドキュメントを参照してください。

* [**01_Overview (全体像・構築計画書)**](./docs/01_overview.md) - プロジェクトの目的、5段階のプラン設計、ロードマップ。
* [**02_Architecture (詳細設計)**](./docs/02_architecture.md) - Hono / Cloudflare Workers / D1 の連携と実行境界の設計。
* [**03_Database_Schema (データベース)**](./docs/03_database_schema.md) - スキーマ定義、ER図、シードデータの運用。
* [**04_Service_Category (サービス設計)**](./docs/04_service_category.md) - カテゴリ定義とデータの成長フェーズ（Level 0〜2）。

---

## 1. 開発・運用コマンド

### アプリケーション実行
- `npm run dev`: ローカル開発サーバー（Wrangler Pages/Workers）の起動
- `npm run deploy`: Cloudflare 本番環境へのデプロイ
- `npx wrangler tail`: 本番環境のリアルタイムログ監視

### データベース運用 (D1)
- `./src/db/apply_seeds.sh --local \`: ローカルDBの初期化・シード投入
- `node scripts/convert_stb.js`: 生テキストデータからスタバ店舗SQLの生成

---

## 2. プロジェクト構造と役割：実行境界の厳格な分離

「どこで動いているコードか」を物理的に判別し、ランタイムエラーとスパゲッティ化を防止します。

### ■ アプリケーション・コア (`src/`)

| フォルダ / 拡張子 | 役割 (責務) | 実行環境 | 不具合時の調査対象 |
| :--- | :--- | :--- | :--- |
| `*.server.ts` | **機密・DB操作**。D1, Bindings, 認証。 | Server | データ欠如、SQLエラー、500エラー |
| `*.client.ts` | **動的・Fetch**。イベント, UI更新。 | Browser | ボタン無反応、画面更新不良 |
| `*.ui.tsx` | **純粋な見た目**。ロジック抜きのJSX。 | Shared(SSR) | レイアウト崩れ、表示ミス |
| `services/` | データアクセス層（D1等との疎通）。 | Server | DBクエリロジック、データ整合性 |
| `hooks/` | フロントエンド論理（状態管理）。 | Browser | UIの状態遷移、非同期通信管理 |
| `types/` | データの法律。型定義(Zod)とSchema。 | Shared | 型不一致、バリデーション失敗 |

---

## 3. データの成長モデル (Data Evolution)

本プロジェクトのコア・コンセプトです。データは以下のフェーズを経て「自律」します。

1. **初期シード期 (Level 0)**: 外部APIより最小限の情報を取得（名称・住所のみ）。
2. **データ昇華期 (Level 1)**: 有料ユーザー（Editor Pro）やオーナーによる「確定・修正」アクションにより、**自社資産**として定義を上書き。
3. **自律稼働期 (Level 2)**: 特定の基盤に依存しない、ALETHEIA独自の高密度データベースとして運用。

---

## 4. 開発フロー：三段階のステップ

1. **[思考] Sandbox**: `src/_sandbox/` でロジックの断片やAPI単体テストを試作。
2. **[検証] Proto (本リポジトリ)**: 正規フォルダ（`src/services`等）に組み込み、システム整合性を確認。
3. **[昇格] Production**: 検証済みのコードを本番用リポジトリへ移植。

---

## 5. デバッグと疎通の掟 (The Rule of Traceability)

### ① 最小経路の優先 (Atomic First)
複雑なHookを作る前に、必ず API エンドポイント単体でDBとの疎通を確認すること。画面が動かない時は、まずブラウザで直接 `/api/xxx` を叩き、生データを確認すること。

### ② ログの指紋 (Log Signatures)
調査場所を即座に特定するため、ログに接頭辞を付ける。
- `console.log("[Server:API] ...")` -> サーバー・DB側の問題（Wranglerログを確認）
- `console.log("[Client:Hook] ...")` -> ブラウザ側の問題（DevToolsを確認）

### ③ 実行境界の死守
`D1Database` 等のサーバー専用オブジェクトを `*.client.ts` に持ち込まない。ブラウザは常に API を経由する「疎結合」を維持すること。

---

## 6. セキュリティ・ポリシー
- **秘匿情報の隔離**: APIキーやシークレットは `wrangler secret put` で管理。
- **バリデーションの二重化**: 静的な `TypeScript` で開発ミスを防ぎ、動的な `Zod` で不正データからシステムを守る。

---

# ALETHEIA 開発プロジェクト

## ■ 完了済（基盤・認証・検証）
- [x] **基盤**: D1スキーマ策定・スタバ1,887件投入・同期自動化
- [x] **認証**: Google OAuth2実装・セッション管理構築
- [x] **安全性**: 論理削除導入・CSRF対策 (`SameSite: Lax`)
- [x] **UI/位置**: 高密度リスト表示・CF Edge位置判定の検証完了
- [x] **DB再設計**: 住所を都道府県・市区町村に物理分離
- [x] **位置統合**: 位置情報は初期値として表示しない（提案型）設計への変更

---

## ■ 今後の重点開発（1日1タスク目安）

### Phase 1: データ構造の「本質的」刷新
- [ ] **タスク1: DB基盤の再構築（Schema & Migration）**
  - カテゴリと特徴の物理分離、および住所データの「市区町村/町域」への構造化保存。
- [ ] **タスク2: データ正規化とクレンジングの実行**
  - 既存の全店舗データを新構造へ移行。この時点で表示上の住所問題（番地削除等）を根本解決。
- [ ] **タスク3: エリア別店舗数集計APIの実装**
  - 分離した住所カラムを使い、店舗実数に基づいた動的なエリア選択肢を生成。
  
### Phase 2: モバイルネイティブUIへの転換
- [ ] **タスク4**: フルスクリーン遷移型ドリルダウン（1画面占有・スライド遷移）の骨組み実装
- [ ] **タスク5**: エントランスUI実装（現在地から探す / 全国から探す の分岐）
- [ ] **タスク6**: 「特徴」タグの複数選択（チップス形式）UIの実装
- [ ] **タスク7**: 検索条件の即時反映（決定ボタンなし・非同期通信）の実装

### Phase 3: 利便性・保存機能
- [ ] **タスク8**: お気に入り登録・解除のAPIおよびフロントエンド実装
- [ ] **タスク9**: マイページ（保存済みリスト表示）のプロトタイプ作成
- [ ] **タスク10**: 主要駅（小岩等）マスタの試験投入と「駅検索」の結合テスト

### Phase 4: 公開・最終調整
- [ ] **タスク11**: モバイル実機での最終操作性デバッグ（指の動線チェック）
- [ ] **タスク12**: `aletheia.shizentaiga.com` への本番デプロイ
- [ ] **タスク13**: 5/01 仮サービスイン

---

## ■ 将来余裕があったら対応（保留・バックログ）
- [ ] **視覚化**: Maps Embed APIの完全統合とAPIキー盗用対策
- [ ] **高度な地理検索**: 現在地からの半径演算によるソート機能
- [ ] **駅/路線マスタの全網羅**: 全国レベルの駅データ自動更新
- [ ] **検索負荷の極限最適化**: 数万件規模を見据えたインデックス再設計

---

## 開発の指針（監査フィードバックより）
- **「構造は厳密に、体験は自由に」**
- **UI**: 探索させるな、選択させろ。フルスクリーン遷移を基本とする。
- **データ**: DBに実在するエリアのみ表示。期待値調整のために件数を添える。
- **ロジック**: カテゴリ（本質）と特徴（条件）を分離して管理。


## ファイル一覧

```
├── scripts/                # 【データパイプライン】
│   ├── fetch_and_convert_stb.js # 外部データの取得から変換までの一括実行
│   ├── convert_starbucks.js     # Starbucks店舗データのSQL変換ロジック
│   └── logs/                    # 生成されたSQL等の作業ログ (gitignore)
│
└── src/
    ├── index.tsx           # 【メインルーティング・API】
    │                        # ページ全体のレンダリングおよびHTMX検索エンドポイントの制御。
    │                        # 検索結果の部分更新には SearchHeader を利用。
    ├── renderer.tsx        # 【HTML基盤定義】
    │                        # 全ページ共通のHTMLメタデータ、外部アセット読み込みの定義。
    │
    ├── pages/              # 【画面テンプレート】
    │   └── Top.tsx         # メインポータルのUI定義。
    │                        # Viewに徹した構造で、ロジックは外部コンポーネントに委譲。
    │
    ├── components/         # 【再利用可能なUI部品】
    │   ├── CafeCard.tsx    # 店舗情報のカード表示。
    │   ├── DebugMonitor.tsx # 内部ステータス（セッション・位置情報）の可視化。
    │   ├── HeaderArea.tsx  # ナビゲーション・ユーザーインターフェース。
    │   ├── SearchSection.tsx # 検索入力およびフィルタリングUI。
    │   ├── SearchHeader.tsx # 検索結果件数等の「検索ステータス」表示。
    │   └── SearchLogic.tsx  # フロントエンドでの動的制御（ドリルダウン等）のJS集約。
    │
    ├── db/                 # 【データアクセス層】
    │   ├── schema.sql      # D1データベースのテーブル設計。
    │   ├── queries.ts      # 型安全なデータ取得関数の定義（Repository層）。
    │   ├── apply_seeds.sh  # 初期データのバッチ投入スクリプト。
    │   └── seed/           # 環境別・チェーン別の初期データSQL群。
    │
    ├── lib/                # 【基盤ロジック】
    │   ├── auth.ts         # Google OAuth2 認証とセッション管理。
    │   └── constants.ts    # マスターデータ、UI文言、システム定数の管理。
    │
    ├── styles/             # 【デザインシステム】
    │   └── theme.ts        # 共通のデザイン変数（余白、色、フォント）の定義。
    │
    └── _sandbox/           # 【デバッグ環境】
        └── _router.ts      # 新機能のプロトタイプ検証用領域。
```